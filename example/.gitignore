# Generated by seed_example.py, deterministic from SEED. Not committed: chDB
# rewrites its directory on open, so a committed copy drifts on every run and
# shows up as spurious diffs.
warehouse/

# Generated by the pipeline (annotate -> eval). Regenerating them is the point of
# the example; committing them would let it pass without ever running.
out/
